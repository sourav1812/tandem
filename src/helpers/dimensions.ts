import {Dimensions} from 'react-native';
const {width: screenWidth, height: screenHeight} = Dimensions.get('screen');

const width = {
  wtable: (screenWidth < screenHeight ? screenWidth : screenHeight) / 1.9,
  wi1: (screenWidth < screenHeight ? screenWidth : screenHeight) * 0.1,
  wi2: (screenWidth < screenHeight ? screenWidth : screenHeight) * 0.2,
  wi3: (screenWidth < screenHeight ? screenWidth : screenHeight) * 0.3,
  wi4: (screenWidth < screenHeight ? screenWidth : screenHeight) * 0.4,
  wi5: (screenWidth < screenHeight ? screenWidth : screenHeight) * 0.5,
  wi6: (screenWidth < screenHeight ? screenWidth : screenHeight) * 0.6,
  wi7: (screenWidth < screenHeight ? screenWidth : screenHeight) * 0.7,
  wi8: (screenWidth < screenHeight ? screenWidth : screenHeight) * 0.8,
  wi9: (screenWidth < screenHeight ? screenWidth : screenHeight) * 0.9,
  w1: (screenWidth < screenHeight ? screenWidth : screenHeight) / 800,
  w2: (screenWidth < screenHeight ? screenWidth : screenHeight) / 400,
  w3: (screenWidth < screenHeight ? screenWidth : screenHeight) / 266.66,
  w4: (screenWidth < screenHeight ? screenWidth : screenHeight) / 200,
  w5: (screenWidth < screenHeight ? screenWidth : screenHeight) / 160,
  w6: (screenWidth < screenHeight ? screenWidth : screenHeight) / 133.33,
  w7: (screenWidth < screenHeight ? screenWidth : screenHeight) / 114.28,
  w8: (screenWidth < screenHeight ? screenWidth : screenHeight) / 100,
  w9: (screenWidth < screenHeight ? screenWidth : screenHeight) / 88.88,
  w10: (screenWidth < screenHeight ? screenWidth : screenHeight) / 80,
  w11: (screenWidth < screenHeight ? screenWidth : screenHeight) / 72.72,
  w12: (screenWidth < screenHeight ? screenWidth : screenHeight) / 66.66,
  w13: (screenWidth < screenHeight ? screenWidth : screenHeight) / 61.53,
  w14: (screenWidth < screenHeight ? screenWidth : screenHeight) / 57.14,
  w15: (screenWidth < screenHeight ? screenWidth : screenHeight) / 53.33,
  w16: (screenWidth < screenHeight ? screenWidth : screenHeight) / 50,
  w17: (screenWidth < screenHeight ? screenWidth : screenHeight) / 47.05,
  w18: (screenWidth < screenHeight ? screenWidth : screenHeight) / 44.44,
  w19: (screenWidth < screenHeight ? screenWidth : screenHeight) / 42.1,
  w20: (screenWidth < screenHeight ? screenWidth : screenHeight) / 40,
  w21: (screenWidth < screenHeight ? screenWidth : screenHeight) / 38.09,
  w22: (screenWidth < screenHeight ? screenWidth : screenHeight) / 36.36,
  w23: (screenWidth < screenHeight ? screenWidth : screenHeight) / 34.78,
  w24: (screenWidth < screenHeight ? screenWidth : screenHeight) / 33.33,
  w25: (screenWidth < screenHeight ? screenWidth : screenHeight) / 32,
  w30: (screenWidth < screenHeight ? screenWidth : screenHeight) / 26.66,
  w35: (screenWidth < screenHeight ? screenWidth : screenHeight) / 22.85,
  w40: (screenWidth < screenHeight ? screenWidth : screenHeight) / 20,
  w45: (screenWidth < screenHeight ? screenWidth : screenHeight) / 17.77,
  w50: (screenWidth < screenHeight ? screenWidth : screenHeight) / 16,
  w55: (screenWidth < screenHeight ? screenWidth : screenHeight) / 14.54,
  w60: (screenWidth < screenHeight ? screenWidth : screenHeight) / 13.33,
  w65: (screenWidth < screenHeight ? screenWidth : screenHeight) / 12.3,
  w70: (screenWidth < screenHeight ? screenWidth : screenHeight) / 11.42,
  w75: (screenWidth < screenHeight ? screenWidth : screenHeight) / 10.66,
  w80: (screenWidth < screenHeight ? screenWidth : screenHeight) / 10,
  w85: (screenWidth < screenHeight ? screenWidth : screenHeight) / 9.41,
  w90: (screenWidth < screenHeight ? screenWidth : screenHeight) / 8.88,
  w95: (screenWidth < screenHeight ? screenWidth : screenHeight) / 8.42,
  w100: (screenWidth < screenHeight ? screenWidth : screenHeight) / 8,
  w110: (screenWidth < screenHeight ? screenWidth : screenHeight) / 7.2,
  w120: (screenWidth < screenHeight ? screenWidth : screenHeight) / 6.66,
  w130: (screenWidth < screenHeight ? screenWidth : screenHeight) / 6.15,
  w140: (screenWidth < screenHeight ? screenWidth : screenHeight) / 5.71,
  w150: (screenWidth < screenHeight ? screenWidth : screenHeight) / 5.33,
  w160: (screenWidth < screenHeight ? screenWidth : screenHeight) / 5,
  w170: (screenWidth < screenHeight ? screenWidth : screenHeight) / 4.7,
  w180: (screenWidth < screenHeight ? screenWidth : screenHeight) / 4.44,
  w190: (screenWidth < screenHeight ? screenWidth : screenHeight) / 4.21,
  w200: (screenWidth < screenHeight ? screenWidth : screenHeight) / 4,
  w210: (screenWidth < screenHeight ? screenWidth : screenHeight) / 3.8,
  w220: (screenWidth < screenHeight ? screenWidth : screenHeight) / 3.63,
  w230: (screenWidth < screenHeight ? screenWidth : screenHeight) / 3.47,
  w240: (screenWidth < screenHeight ? screenWidth : screenHeight) / 3.33,
  w250: (screenWidth < screenHeight ? screenWidth : screenHeight) / 3.2,
  w260: (screenWidth < screenHeight ? screenWidth : screenHeight) / 3.07,
  w270: (screenWidth < screenHeight ? screenWidth : screenHeight) / 2.96,
  w280: (screenWidth < screenHeight ? screenWidth : screenHeight) / 2.85,
  w290: (screenWidth < screenHeight ? screenWidth : screenHeight) / 2.75,
  w300: (screenWidth < screenHeight ? screenWidth : screenHeight) / 2.66,
  w310: (screenWidth < screenHeight ? screenWidth : screenHeight) / 2.58,
  w400: (screenWidth < screenHeight ? screenWidth : screenHeight) / 1.1,
  wMax: screenWidth < screenHeight ? screenWidth : screenHeight,
};

const height = {
  h1: (screenHeight > screenWidth ? screenHeight : screenWidth) / 1232,
  h2: (screenHeight > screenWidth ? screenHeight : screenWidth) / 616,
  h3: (screenHeight > screenWidth ? screenHeight : screenWidth) / 410.66,
  h4: (screenHeight > screenWidth ? screenHeight : screenWidth) / 308,
  h5: (screenHeight > screenWidth ? screenHeight : screenWidth) / 246.4,
  h6: (screenHeight > screenWidth ? screenHeight : screenWidth) / 205.33,
  h7: (screenHeight > screenWidth ? screenHeight : screenWidth) / 176,
  h8: (screenHeight > screenWidth ? screenHeight : screenWidth) / 154,
  h9: (screenHeight > screenWidth ? screenHeight : screenWidth) / 136.88,
  h10: (screenHeight > screenWidth ? screenHeight : screenWidth) / 123.2,
  h11: (screenHeight > screenWidth ? screenHeight : screenWidth) / 112,
  h12: (screenHeight > screenWidth ? screenHeight : screenWidth) / 102.66,
  h13: (screenHeight > screenWidth ? screenHeight : screenWidth) / 74.76,
  h14: (screenHeight > screenWidth ? screenHeight : screenWidth) / 88,
  h15: (screenHeight > screenWidth ? screenHeight : screenWidth) / 82.13,
  h16: (screenHeight > screenWidth ? screenHeight : screenWidth) / 77,
  h17: (screenHeight > screenWidth ? screenHeight : screenWidth) / 72.47,
  h18: (screenHeight > screenWidth ? screenHeight : screenWidth) / 68.44,
  h19: (screenHeight > screenWidth ? screenHeight : screenWidth) / 64.84,
  h20: (screenHeight > screenWidth ? screenHeight : screenWidth) / 61.6,
  h21: (screenHeight > screenWidth ? screenHeight : screenWidth) / 58.66,
  h22: (screenHeight > screenWidth ? screenHeight : screenWidth) / 56,
  h23: (screenHeight > screenWidth ? screenHeight : screenWidth) / 53.56,
  h24: (screenHeight > screenWidth ? screenHeight : screenWidth) / 51.33,
  h25: (screenHeight > screenWidth ? screenHeight : screenWidth) / 49.28,
  h30: (screenHeight > screenWidth ? screenHeight : screenWidth) / 41.06,
  h35: (screenHeight > screenWidth ? screenHeight : screenWidth) / 35.2,
  h40: (screenHeight > screenWidth ? screenHeight : screenWidth) / 30.8,
  h45: (screenHeight > screenWidth ? screenHeight : screenWidth) / 27.37,
  h50: (screenHeight > screenWidth ? screenHeight : screenWidth) / 24.64,
  h55: (screenHeight > screenWidth ? screenHeight : screenWidth) / 22.4,
  h60: (screenHeight > screenWidth ? screenHeight : screenWidth) / 20.53,
  // h62: (screenHeight > screenWidth ? screenHeight : screenWidth) / 18.0,
  h65: (screenHeight > screenWidth ? screenHeight : screenWidth) / 18.95,
  h70: (screenHeight > screenWidth ? screenHeight : screenWidth) / 17.6,
  h75: (screenHeight > screenWidth ? screenHeight : screenWidth) / 16.42,
  h80: (screenHeight > screenWidth ? screenHeight : screenWidth) / 15.4,
  h85: (screenHeight > screenWidth ? screenHeight : screenWidth) / 14.49,
  h90: (screenHeight > screenWidth ? screenHeight : screenWidth) / 13.68,
  h95: (screenHeight > screenWidth ? screenHeight : screenWidth) / 12.96,
  h100: (screenHeight > screenWidth ? screenHeight : screenWidth) / 12.32,
  h110: (screenHeight > screenWidth ? screenHeight : screenWidth) / 11.2,
  h120: (screenHeight > screenWidth ? screenHeight : screenWidth) / 10.26,
  h130: (screenHeight > screenWidth ? screenHeight : screenWidth) / 9.47,
  h140: (screenHeight > screenWidth ? screenHeight : screenWidth) / 8.8,
  h150: (screenHeight > screenWidth ? screenHeight : screenWidth) / 8.21,
  h160: (screenHeight > screenWidth ? screenHeight : screenWidth) / 7.7,
  h170: (screenHeight > screenWidth ? screenHeight : screenWidth) / 7.24,
  h180: (screenHeight > screenWidth ? screenHeight : screenWidth) / 6.84,
  h190: (screenHeight > screenWidth ? screenHeight : screenWidth) / 6.48,
  h200: (screenHeight > screenWidth ? screenHeight : screenWidth) / 6.16,
  h210: (screenHeight > screenWidth ? screenHeight : screenWidth) / 5.86,
  h220: (screenHeight > screenWidth ? screenHeight : screenWidth) / 5.6,
  h230: (screenHeight > screenWidth ? screenHeight : screenWidth) / 5.35,
  h240: (screenHeight > screenWidth ? screenHeight : screenWidth) / 5.13,
  h250: (screenHeight > screenWidth ? screenHeight : screenWidth) / 4.92,
  h255: (screenHeight > screenWidth ? screenHeight : screenWidth) / 4.8,
  h260: (screenHeight > screenWidth ? screenHeight : screenWidth) / 4.73,
  h270: (screenHeight > screenWidth ? screenHeight : screenWidth) / 4.56,
  h280: (screenHeight > screenWidth ? screenHeight : screenWidth) / 4.4,
  h290: (screenHeight > screenWidth ? screenHeight : screenWidth) / 4.24,
  h300: (screenHeight > screenWidth ? screenHeight : screenWidth) / 4.1,
  h310: (screenHeight > screenWidth ? screenHeight : screenWidth) / 3.97,
  h320: (screenHeight > screenWidth ? screenHeight : screenWidth) / 3.85,
  h330: (screenHeight > screenWidth ? screenHeight : screenWidth) / 3.73,
  h340: (screenHeight > screenWidth ? screenHeight : screenWidth) / 3.62,
  h350: (screenHeight > screenWidth ? screenHeight : screenWidth) / 3.52,
  h500: (screenHeight > screenWidth ? screenHeight : screenWidth) / 1.5,
  hmax: screenHeight > screenWidth ? screenHeight : screenWidth,
};

const spacing = {
  h5: (screenWidth < screenHeight ? screenWidth : screenHeight) / 160,
  h6: (screenWidth < screenHeight ? screenWidth : screenHeight) / 133.333,
  h7: (screenWidth < screenHeight ? screenWidth : screenHeight) / 114.28,
  h8: (screenWidth < screenHeight ? screenWidth : screenHeight) / 100,
  h9: (screenWidth < screenHeight ? screenWidth : screenHeight) / 88.88,
  h10: (screenWidth < screenHeight ? screenWidth : screenHeight) / 80,
  h15: (screenWidth < screenHeight ? screenWidth : screenHeight) / 53.33,
  h20: (screenWidth < screenHeight ? screenWidth : screenHeight) / 40,
  h25: (screenWidth < screenHeight ? screenWidth : screenHeight) / 32,
  h30: (screenWidth < screenHeight ? screenWidth : screenHeight) / 26.66,
  h35: (screenWidth < screenHeight ? screenWidth : screenHeight) / 22.85,
  h40: (screenWidth < screenHeight ? screenWidth : screenHeight) / 20,
  h45: (screenWidth < screenHeight ? screenWidth : screenHeight) / 17.77,
  h50: (screenWidth < screenHeight ? screenWidth : screenHeight) / 16,
  h55: (screenWidth < screenHeight ? screenWidth : screenHeight) / 14.54,
  h60: (screenWidth < screenHeight ? screenWidth : screenHeight) / 13.33,
  h65: (screenWidth < screenHeight ? screenWidth : screenHeight) / 12.3,
  h70: (screenWidth < screenHeight ? screenWidth : screenHeight) / 11.42,
  h75: (screenWidth < screenHeight ? screenWidth : screenHeight) / 10.66,
  h80: (screenWidth < screenHeight ? screenWidth : screenHeight) / 10,
  h85: (screenWidth < screenHeight ? screenWidth : screenHeight) / 9.41,
  h90: (screenWidth < screenHeight ? screenWidth : screenHeight) / 8.88,
  h100: (screenWidth < screenHeight ? screenWidth : screenHeight) / 8,
  h150: (screenWidth < screenHeight ? screenWidth : screenHeight) / 5.33,
  h200: (screenWidth < screenHeight ? screenWidth : screenHeight) / 4,
  v0: 0,
  v1: (screenHeight > screenWidth ? screenHeight : screenWidth) / 1232,
  v2: (screenHeight > screenWidth ? screenHeight : screenWidth) / 616,
  v3: (screenHeight > screenWidth ? screenHeight : screenWidth) / 410.66,
  v4: (screenHeight > screenWidth ? screenHeight : screenWidth) / 308,
  v5: (screenHeight > screenWidth ? screenHeight : screenWidth) / 246.4,
  v6: (screenHeight > screenWidth ? screenHeight : screenWidth) / 205.33,
  v7: (screenHeight > screenWidth ? screenHeight : screenWidth) / 176,
  v8: (screenHeight > screenWidth ? screenHeight : screenWidth) / 154,
  v9: (screenHeight > screenWidth ? screenHeight : screenWidth) / 136.88,
  v10: (screenHeight > screenWidth ? screenHeight : screenWidth) / 123.2,
  v11: (screenHeight > screenWidth ? screenHeight : screenWidth) / 112,
  v12: (screenHeight > screenWidth ? screenHeight : screenWidth) / 102.66,
  v13: (screenHeight > screenWidth ? screenHeight : screenWidth) / 74.76,
  v14: (screenHeight > screenWidth ? screenHeight : screenWidth) / 88,
  v15: (screenHeight > screenWidth ? screenHeight : screenWidth) / 82.13,
  v16: (screenHeight > screenWidth ? screenHeight : screenWidth) / 77,
  v17: (screenHeight > screenWidth ? screenHeight : screenWidth) / 72.47,
  v18: (screenHeight > screenWidth ? screenHeight : screenWidth) / 68.44,
  v19: (screenHeight > screenWidth ? screenHeight : screenWidth) / 64.84,
  v20: (screenHeight > screenWidth ? screenHeight : screenWidth) / 61.6,
  v21: (screenHeight > screenWidth ? screenHeight : screenWidth) / 58.66,
  v22: (screenHeight > screenWidth ? screenHeight : screenWidth) / 56,
  v23: (screenHeight > screenWidth ? screenHeight : screenWidth) / 53.56,
  v24: (screenHeight > screenWidth ? screenHeight : screenWidth) / 51.33,
  v25: (screenHeight > screenWidth ? screenHeight : screenWidth) / 49.28,
  v30: (screenHeight > screenWidth ? screenHeight : screenWidth) / 41.06,
  v35: (screenHeight > screenWidth ? screenHeight : screenWidth) / 35.2,
  v40: (screenHeight > screenWidth ? screenHeight : screenWidth) / 30.8,
  v45: (screenHeight > screenWidth ? screenHeight : screenWidth) / 27.37,
  v50: (screenHeight > screenWidth ? screenHeight : screenWidth) / 24.64,
  v55: (screenHeight > screenWidth ? screenHeight : screenWidth) / 22.4,
  v60: (screenHeight > screenWidth ? screenHeight : screenWidth) / 20.53,
  v65: (screenHeight > screenWidth ? screenHeight : screenWidth) / 18.95,
  v70: (screenHeight > screenWidth ? screenHeight : screenWidth) / 17.6,
  v75: (screenHeight > screenWidth ? screenHeight : screenWidth) / 16.42,
  v80: (screenHeight > screenWidth ? screenHeight : screenWidth) / 15.4,
  v85: (screenHeight > screenWidth ? screenHeight : screenWidth) / 14.49,
  v90: (screenHeight > screenWidth ? screenHeight : screenWidth) / 13.68,
  v100: (screenHeight > screenWidth ? screenHeight : screenWidth) / 12.96,
  v110: (screenHeight > screenWidth ? screenHeight : screenWidth) / 11.2,
  v120: (screenHeight > screenWidth ? screenHeight : screenWidth) / 10.26,
  v130: (screenHeight > screenWidth ? screenHeight : screenWidth) / 9.47,
  v140: (screenHeight > screenWidth ? screenHeight : screenWidth) / 8.8,
  v150: (screenHeight > screenWidth ? screenHeight : screenWidth) / 8.21,
  v160: (screenHeight > screenWidth ? screenHeight : screenWidth) / 7.7,
  v170: (screenHeight > screenWidth ? screenHeight : screenWidth) / 7.24,
  v180: (screenHeight > screenWidth ? screenHeight : screenWidth) / 6.84,
  v190: (screenHeight > screenWidth ? screenHeight : screenWidth) / 6.48,
  v200: (screenHeight > screenWidth ? screenHeight : screenWidth) / 6.16,
};

export {width, height, spacing, screenWidth, screenHeight};
